apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-service-config
data:
  REDIS_HOST: "localhost"
  REDIS_PORT: "6379"
  MINIO_ENDPOINT: "localhost"
  MINIO_PORT: "9000"
  KAFKA_BROKERS: "localhost:9092"
